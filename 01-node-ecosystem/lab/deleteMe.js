describe('Greet Module', () => {
    it('describes what is being tested', () => {

        // run some commands

        expect('something').toBe('the expected result');

    });

});

module.exports = {
    addFunction = (a, b) => {

    },

};


if (NaN(a)) {

}